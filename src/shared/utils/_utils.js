import {
    checkVRChatCache,
    deleteVRChatCache,
    displayLocation,
    extractFileId,
    extractFileVersion,
    extractVariantVersion,
    getAvailablePlatforms,
    getLaunchURL,
    getPrintFileName,
    getPrintLocalDate,
    isFriendOnline,
    isRealInstance,
    parseLocation
} from './index';

const _utils = {
    getAvailablePlatforms,
    deleteVRChatCache,
    checkVRChatCache,
    getLaunchURL,
    extractFileId,
    extractFileVersion,
    extractVariantVersion,
    isRealInstance,
    displayLocation,
    parseLocation,
    getPrintFileName,
    getPrintLocalDate,
    isFriendOnline
};

export { _utils };
