import {
    checkVRChatCache,
    deleteVRChatCache,
    displayLocation,
    extractFileId,
    extractFileVersion,
    extractVariantVersion,
    getAvailablePlatforms,
    getLaunchURL,
    getPrintFileName,
    getPrintLocalDate,
    isFriendOnline,
    isRealInstance,
    parseLocation,
    getBundleLocation
} from './index';

const _utils = {
    getAvailablePlatforms,
    deleteVRChatCache,
    checkVRChatCache,
    getLaunchURL,
    extractFileId,
    extractFileVersion,
    extractVariantVersion,
    isRealInstance,
    displayLocation,
    parseLocation,
    getPrintFileName,
    getPrintLocalDate,
    isFriendOnline,
    getBundleLocation
};

export { _utils };
