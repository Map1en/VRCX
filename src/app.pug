doctype html
#x-app.x-app(@dragenter.prevent @dragover.prevent @drop.prevent)
    //- ### Login ###
    LoginPage(v-if="!this.store.auth.isLoggedIn" v-on="loginPageEvent")

    VRCXUpdateDialog

    template(v-if='this.store.auth.isLoggedIn')
        //- ### Menu ###
        NavMenu(ref='menu')

        //- ### Sidebar ###
        Sidebar

        //- ### Tabs ###
        FeedTab

        GameLogTab(v-on='gameLogTabEvent')

        PlayerListTab

        SearchTab

        FavoritesTab

        FriendLogTab

        ModerationTab

        NotificationTab

        FriendListTab

        ChartsTab

        ProfileTab

        SettingsTab(v-on='settingsTabEvent')

        //- ## Dialogs ## -\\
        PreviousInstancesInfoDialog

        FriendImportDialog

        WorldImportDialog

        AvatarImportDialog

        ChooseFavoriteGroupDialog

        LaunchDialog

        UserDialog

        WorldDialog

        GroupDialog

        AvatarDialog

        FullscreenImageDialog

        LaunchOptionsDialog

        EditInviteMessageDialog

        VRChatConfigDialog

        PrimaryPasswordDialog

        GalleryDialog
